from setuptools import setup, find_packages

setup(
    name="simbaid",
    version="0.1.0",
    description="SimbaID Backend: Voice-Biometric DID with FastAPI",
    packages=find_packages(),
    install_requires=[],  # managed via pyproject
)
